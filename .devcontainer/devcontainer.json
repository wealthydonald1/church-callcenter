{
  "name": "Flutter",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y curl git unzip xz-utils zip libglu1-mesa && cd ~ && git clone https://github.com/flutter/flutter.git -b stable || true && echo 'export PATH=\"$PATH:$HOME/flutter/bin\"' >> ~/.bashrc",
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.dart-code",
        "Dart-Code.flutter"
      ]
    }
  }
}
